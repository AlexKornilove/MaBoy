# NSPU Schedule Bot

Telegram-бот для просмотра расписания занятий НГПУ (Новосибирский государственный педагогический университет).

## Возможности

- 📅 Просмотр расписания на сегодня и на неделю
- 📆 Отображение текущей учебной недели
- 🔍 Выбор группы по коду, названию или через интерактивный список
- 🔔 Ежедневная рассылка расписания по подписке
- 🚀 Интеграция с Welcome Hub

## Установка

### 1. Получение токена бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен

### 2. Настройка проекта

```bash
cd ScheduleBot

# Установка зависимостей
npm install

# Создание файла конфигурации
copy .env.example .env

# Отредактируйте .env и вставьте токен бота
notepad .env
```

### 3. Запуск

```bash
# Автономный запуск
npm start

# Или через Hub (запускается автоматически)
cd ../hub
npm start
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/group` | Выбрать или изменить группу |
| `/today` | Расписание на сегодня |
| `/schedule` | Расписание на неделю |
| `/subscribe` | Включить ежедневную рассылку |
| `/unsubscribe` | Отключить рассылку |
| `/help` | Справка |

## Структура проекта

```
ScheduleBot/
├── config/
│   ├── selectors.js   # CSS-селекторы (менять при изменении сайта)
│   └── settings.js    # Настройки бота
├── data/
│   └── users.json     # Хранение пользователей
├── src/
│   ├── index.js       # Точка входа
│   ├── bot.js         # Telegram бот
│   ├── parsers/       # HTML-парсеры
│   ├── services/      # Сервисы (хранение, планировщик)
│   └── utils/         # Утилиты (форматирование)
├── .env.example
├── package.json
└── README.md
```

## Источник данных

Данные загружаются с официального сайта расписаний НГПУ:
- https://schedule.nspu.ru

## Устойчивость к изменениям

Все CSS-селекторы вынесены в `config/selectors.js`. При изменении структуры сайта расписаний достаточно обновить селекторы в этом файле.

## Лицензия

ISC
